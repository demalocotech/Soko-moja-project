import 'package:flutter/material.dart';

class AttributeTabScreen extends StatelessWidget {
  const AttributeTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Attribute screen'),
    );
  }
}
