import 'package:flutter/material.dart';

class ShippingTabScreen extends StatelessWidget {
  const ShippingTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('shipping screen'),
    );
  }
}
