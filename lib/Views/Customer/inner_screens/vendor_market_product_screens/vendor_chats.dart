import 'package:flutter/material.dart';

class VendorChats extends StatelessWidget {
  const VendorChats({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('chats'),
    );
  }
}
